Q. why props?
Ans-make a component to make it reusable.

Q.can we pass custom props(eg "blahblahblah={true}") to a native 
DOM element?{eg <div blahblahblah={true}>)

Ans- no bcz the jsx we use to describe native DOM elements will only be turned into 
real dom element by react and real dom elementonly have the propertes specified in the 
html specification.

Qhow do i receive prop in a component?
function NavBar(props){
    return(
        <header></header>
    )
}

Q. datatype of prop when component receive it?
Ans-object

Q. what does ".map()" array method do?
  vanilla js array method ,returns new array. whever gets returned frim the callback functioon 
  provided is placed at same index in the array.
  usually we take the item from the original array and modify them in some ways.


Q.what do we ususally use  '.map()' in react for?
convert array of ra data into an arrayof jsx element that can be 
displayed on page.

Q.why is using '.map()' better than just creating the component manually by typing them out?
1. we usually dont have whole data ahead of time when were building
2.by using it, it makes the code more self sustaining.
3. easy to modify